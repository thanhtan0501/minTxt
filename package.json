{
  "scripts": {
    "start": "http-server ./src",
    "predeploy": "cp -r src/ dist/",
    "deploy": "gh-pages -d dist",
    "postdeploy": "rm dist -r -f"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
