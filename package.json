{
  "scripts": {
    "start": "http-server ./src",
    "predeploy": "rm dist -r -f && mkdir dist && cp src/*.* dist",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
