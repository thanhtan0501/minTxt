{
  "scripts": {
    "start": "http-server ./src",
    "cleanup": "rm dist -r -f",
    "predeploy": "cleanup && cp -r src/ dist/",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
