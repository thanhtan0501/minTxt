{
  "scripts": {
    "start": "parcel index.html",
    "predeploy": "rmdir dist -Force -Recursive; parcel build index.html --public-url /mintxt",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "parcel": "^1.12.4"
  }
}
